# Guía de Contribución del Equipo

1. Nunca hagas commits directos a la rama `main`.
2. Crea ramas con el formato: `feature/nombre-tarea` o `fix/descripcion-error`.
3. Asegúrate de que los mensajes de commit sigan el estándar *Conventional Commits* (ej. `feat: añade botón de login`).
